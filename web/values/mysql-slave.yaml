fullnameOverride: mysql-slave

podSecurityContext:
  runAsUser: 1003
  runAsGroup: 1004

nodeSelector:
  server-role-web: "true"
  domain-crespel-me: "true"

config:
  slave.cnf: |
    [mysqld]
    binlog_format = mixed
    expire_logs_days = 7
    log-bin = mysql-bin
    max_allowed_packet = 67108864
    server-id = 2
    read-only

volumes:
  data:
    hostPath:
      path: /home/domains/crespel.me/mysql-slave
      type: DirectoryOrCreate
