fullnameOverride: logstash

nodeSelector:
  server-role-web: "true"
  domain-crespel-me: "true"

env:
  ELASTICSEARCH_CACERT: /usr/share/logstash/secret/elasticsearch-ca.crt
  ELASTICSEARCH_HOSTS: https://elasticsearch-es-http.infra.svc:9200
  ELASTICSEARCH_USERNAME: elastic
  LS_JAVA_OPTS: -Xms512m -Xmx512m
  XPACK_MONITORING_ENABLED: "false"

volumes:
  - name: data
    hostPath:
      path: /home/domains/crespel.me/logstash/data
      type: DirectoryOrCreate
  - name: domain
    hostPath:
      path: /home/domains/crespel.me
      type: DirectoryOrCreate
volumeMounts:
  - mountPath: /usr/share/logstash/data
    name: data
  - mountPath: /home/domains/crespel.me
    name: domain
    readOnly: true
